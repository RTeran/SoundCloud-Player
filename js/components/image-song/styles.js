import { StyleSheet } from 'react-native'


module.exports = StyleSheet.create({
    image: {
        height: 60,
        width: 60,
        flexDirection: 'row',
        justifyContent: 'center',
    }
});
